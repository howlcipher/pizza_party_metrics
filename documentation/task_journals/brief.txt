Objective:
Add a leaderboard or benchmark comparison component using the `industry_profile` array from `/run/media/system/tallgeese/dev/pizza_party_metrics/src/data/advanced_collaboration_insights.json`.

Context:
This should allow users to compare their selected filters against baseline industry averages for Focus Hours and Meeting Overhead.
The `Dashboard` component currently has a `filters` state. You can pass `filters.industry` as a prop to highlight the selected industry.

Files involved:
- `/run/media/system/tallgeese/dev/pizza_party_metrics/src/components/Charts/IndustryBenchmarksChart.tsx` (Create this file)
- `/run/media/system/tallgeese/dev/pizza_party_metrics/src/components/Dashboard/Dashboard.tsx` (Add `IndustryBenchmarksChart` lazy load and embed it in the UI, below `DemographicsChart`)
- `/run/media/system/tallgeese/dev/pizza_party_metrics/src/data/advanced_collaboration_insights.json` (Source of the data)

Tasks:
1. Create `/run/media/system/tallgeese/dev/pizza_party_metrics/src/components/Charts/IndustryBenchmarksChart.tsx`. It should import `advanced_collaboration_insights.json` and read the `industry_profile` data. Render a bar chart or leaderboard UI comparing `avg_focus_hours` and `avg_meeting_overhead` across industries. Sort the data nicely.
2. In `/run/media/system/tallgeese/dev/pizza_party_metrics/src/components/Dashboard/Dashboard.tsx`, lazy import `IndustryBenchmarksChart` and place it inside the grid below `DemographicsChart`. You can pass `selectedIndustry={filters.industry}`. Make it span an appropriate number of columns.
3. Keep styling consistent with existing charts (e.g., Recharts, TailwindCSS).

Constraints:
- Only edit the mentioned files. Do not modify other charts.
- Use Recharts for charts if you need to, or just Tailwind for a leaderboard.
